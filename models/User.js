export default class User {
  constructor(email, fName, lName, pw) {
    this.email = email; // String
    this.fName = fName; // String
    this.lName = lName; // String
    this.pw = pw; // String
  }
}
